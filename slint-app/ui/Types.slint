export struct CurrentSong {
    title: string,
    artists: string,
    position: int,
    duration: int,
    is_paused: bool,
    song_uuid: string,
    album_art: image,
}
export struct Song {
    title: string,
    artists: string,
    album_art: image,
    id: string,
}
